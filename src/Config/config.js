const config = {
  client: 'mssql',
  connection: {
    host : 'regulus.academico.cotuca.unicamp.br',
    user : 'BD20351',
    password : 'BD20351',
    database : 'BD20351'
  }
}

module.exports = config;